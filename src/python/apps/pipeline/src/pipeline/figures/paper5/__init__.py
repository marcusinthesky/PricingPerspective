"""Paper 5 publication figures and generated numbers.

Import the entry point you need directly from its role submodule
(``from pipeline.figures.paper5.figures import render_paper5_figures``) so a
stage's dependency closure stays scoped to one role -- do NOT re-export them here.
"""
