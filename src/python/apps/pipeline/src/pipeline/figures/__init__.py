"""Publication figure + generated-number rendering, split by paper.

Import the entry point you need directly from its submodule (for example,
``from pipeline.figures.paper3.figures import render_paper3_figures``) so a
stage's dependency closure stays scoped to one paper -- do NOT re-export them here.
"""
