---
description: Usage notes for the vendored binaries pinned in the nix dev shell (just, prek, dotenvx).
---

# infra/nix/docs/ — vendored binary usage notes

Usage notes for the vendored binaries pinned in the nix dev shell (`just`, `prek`, `dotenvx`).

<!-- insitu:begin gittree
id = "gittree"
path = "infra/nix/docs"
depth = 1
-->

```text
.
├── dotenvx # dotenvx — encrypted environment variable management for the monorepo
├── just # just — the polyglot command runner for the monorepo
└── prek # prek — Rust drop-in replacement for pre-commit
```

<!-- insitu:end -->
