# Historical extraction resource

`wikifil.pl` is the public-domain extractor frozen by the original FastText
vintage workflow. It is retained to document how the existing
`data/.encoder_vintage/corpus/*.txt` files were derived.

EttaX does not execute this script. Corpus acquisition and XML processing are
outside the app's boundary, and the training queue must not regenerate those
211 GB of existing artifacts.
